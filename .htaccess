RewriteEngine On
RewriteBase /

# Don't proxy if file/directory exists in public_html
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Proxy everything else to Gunicorn on port 8000
RewriteRule ^(.*)$ http://127.0.0.1:8000/$1 [P,L]